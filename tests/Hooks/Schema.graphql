type Query {
    viewer: Viewer!
}

type Viewer {
    login: String!
    projects: [Project!]!
}

type Project {
    id: ID!
    name: String!
    description: String
    creator: User!
}

type User {
    id: ID!
}
