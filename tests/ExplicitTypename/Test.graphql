query Test {
    viewer {
        __typename
        name
        ...UserDetails
        ...on Application {
            __typename
            url
            name
        }
    }
}

fragment UserDetails on User {
    login
}
