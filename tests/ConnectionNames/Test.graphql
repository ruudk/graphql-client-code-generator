query Test {
    viewer {
        id
        name
        role
        createdAt
    }
    projects {
        edges {
            cursor
            node {
                id
                name
                status
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            startCursor
            endCursor
        }
    }
}