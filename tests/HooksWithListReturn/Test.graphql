query Test {
    viewer {
        login
        projects {
            name
            contributorIds

            contributors @hook(name: "findUsersByIds", input: ["contributorIds"])
        }
    }
}
