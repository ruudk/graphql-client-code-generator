query Test {
    viewer {
        login
        projects {
            name
            description
            creator {
                id
            }

            user @hook(name: "findUserById", input: ["creator.id"])
        }
    }
}
